<% if comment.body.present? %>

  <p>
    <%= comment.commenter %>:<br/>
    <em style="color: #0000FF;"><%= raw(comment.body) %></em>
  </p>

  <p>
    <%= link_to 'Delete', [comment.post, comment],
                 :confirm => 'Are you sure?',
                 :method => :delete, :class => 'btn-mini btn-info' %>
  </p>
  <hr />

<% end %>
